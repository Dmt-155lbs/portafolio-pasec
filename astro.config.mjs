// @ts-check
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';

// https://astro.build/config
export default defineConfig({
  site: 'https://Dmt-155lbs.github.io',
  base: '/portafolio-pasec',
  integrations: [react()]
});